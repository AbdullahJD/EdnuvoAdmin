# Discovered Components

This is an auto-generated list of components discovered by [nuxt/components](https://github.com/nuxt/components).

You can directly use them in pages and other components without the need to import them.

**Tip:** If a component is conditionally rendered with `v-if` and is big, it is better to use `Lazy` or `lazy-` prefix to lazy load.

- `<FullProjectOffer>` | `<full-project-offer>` (components/fullProjectOffer.vue)
- `<HourProjectOffer>` | `<hour-project-offer>` (components/hourProjectOffer.vue)
- `<PhaseProjectOffer>` | `<phase-project-offer>` (components/phaseProjectOffer.vue)
- `<Search>` | `<search>` (components/search.vue)
- `<Stats>` | `<stats>` (components/stats.vue)
